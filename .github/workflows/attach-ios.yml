name: Attach iOS build

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "ios/**"
      - "capacitor.config.json"
      - ".github/workflows/attach-ios.yml"
      - "package.json"
      - "package-lock.json"
      - "client/**"
      - "script/**"

permissions:
  contents: write

concurrency:
  group: attach-ios-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ios:
    name: Build iOS IPA
    runs-on: macos-14
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Web build
        run: npm run build
        env:
          NODE_ENV: production

      - name: Sync Capacitor iOS
        run: npx cap sync ios

      - name: Build iOS IPA
        run: bash script/ci-ios-build.sh

      - name: Upload build log (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-xcodebuild-log
          path: release-artifacts/xcodebuild.log
          if-no-files-found: ignore

      - name: Upload IPA artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: release-artifacts/Cunoku-1.0.3.ipa
          if-no-files-found: error

      - name: Attach IPA to GitHub Release v1.0.3
        if: success()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          test -f "$GITHUB_WORKSPACE/release-artifacts/Cunoku-1.0.3.ipa"
          gh release upload v1.0.3 \
            "$GITHUB_WORKSPACE/release-artifacts/Cunoku-1.0.3.ipa" \
            --clobber
          gh release edit v1.0.3 --notes-file RELEASE_NOTES_1.0.3.md
