# APKs locais (Android)

Pasta para builds `.apk` gerados localmente (ex.: `Cunoku-1.0.12.apk`).

- **Não versionados** — cobertos por `*.apk` no `.gitignore`
- Releases oficiais ficam em: https://github.com/MatheusPereira64/CunokuGame/releases
